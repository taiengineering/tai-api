fastapi
uvicorn
supabase
psycopg2-binary>=2.9.9
python-dotenv
requests==2.31.0
urllib3==2.0.7
pandas
openpyxl
httpx>=0.27.0
cryptography>=42.0.0
apscheduler
python-multipart
bcrypt
resend
tenacity
xhtml2pdf
jinja2
python-dateutil>=2.8.2
firebase-admin
openai>=1.0.0
sentry-sdk[fastapi]

# psycopg2-binary: 정기결제 직접 SQL(db/direct_sql.py) — PostgREST 스키마캐시
# 우회용. 누락 시 POST /payments/inicis/billing/prepare 가 ModuleNotFoundError로
# 평문 500("Internal Server Error") 반환 → 프론트 JSON 파싱 실패.

# urllib3==2.0.7 + requests==2.31.0: 프록시(OUTBOUND_PROXY) 경유 data.go.kr
# HTTPS 호출이 requests 2.32+ 에서 코드10(INVALID_REQUEST_PARAMETER)로 거부됨(프록시
# CONNECT 구성 변경). 실측 확정: 동일 urllib3(2.0.7)·프록시·키·파라미터에서 requests
# 2.31.0=정상(200), 2.34.2=코드10. 카페24 egress 서버가 2.31.0 이라 정상이었음.
# 종전 #138 은 urllib3 만 핀하고 requests 를 bare 로 둬 2.34.2 로 떠올라 문제 잔존 →
# requests==2.31.0 으로 핀해 확정 해결. (건축물대장/JUSO/KOSHA 프록시 경로 공통)

# === TAI 법령엔진 v3.0 — Track A (2026-05-09 추가) ===
# 형태소 분석기 (한국어). LLM 사용 금지 원칙(§2.1) 대체 도구.
# 라이선스: LGPL v3 — 동적 링크 사용 OK (NOTICE 파일 별도 관리 필요)
kiwipiepy>=0.22.0,<0.24.0
kiwipiepy-model>=0.22.0
playwright

# === WO-8 Gmail 채널 — 서비스계정 도메인 위임 발송/수신 (2026-07-28 추가) ===
# gmail_channel.py 지연 import. 누락 시 GmailError(501)로 안전 폴백(발송 불가).
google-api-python-client>=2.100.0
google-auth>=2.23.0
