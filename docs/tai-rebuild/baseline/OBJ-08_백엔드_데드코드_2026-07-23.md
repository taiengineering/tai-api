# OBJ-08 — 백엔드 데드코드 제거 (미등록 고아 라우터)

일자: 2026-07-23 · 대상: tai-api (main 직접 배포)
커밋: `a12cb3b` — "chore(O8): remove 20 unregistered orphan routers (dead code)"

## 원칙
- 미등록(=router_registry 미포함)이라도 **다른 코드가 import하면 삭제 시 부팅 붕괴**. 따라서 삭제 전 **import 전수 검증**(routers/services/router_registry/main 전역 grep) 필수.
- 엔진(법령·체크)은 별도 워크스트림 → 엔진 경계 폐기버전은 여기서 손대지 않음.

## 검증 방법
`taiengineering/tai-api@main` 얕은 클론 후 861개 `.py` 전수 grep — 각 후보의 import 형탘(`routers.X`, `from routers.X import`, bare-name)를 자기 파일·`*.md` 문서 제외하고 확인. 라우터 간 상대 import 없음 확인 → `routers.X`가 완전한 import 표면.

## 결과

### 삭제 (20개) — 앱 소유 · 코드 import 0
buildings, contacts, posts, teams, roles, workers, uploads, areas, industry,
oauth, auth_oauth, pw_reset, inspection_templates, fix_matching_api,
process_management, emergency_report, safety_reports, tbm_issue,
feature_flags, debug

### 유지 (KEEP) — 등록 라우터/서비스가 import (미등록이나 라이브)
- `construction_catalog_router` ← `routers/construction.py`(등록됨)가 import
- `construction_sites_router` ← `routers/construction.py`(등록) + tests
- `construction_workflow_router` ← `routers/construction.py`(등록) + tests
- `inspection_set_auto` ← `services/legal_engine_svc.py`, `services/construction_svc.py`, `construction_sites_router.py` (서비스 import는 연쇄 → **반드시 유지**)

> 주의: `router_registry/construction.py`는 `routers.construction` 하나만 등록하고, 위 3개 `construction_*_router`는 그 하위 서브라우터. 레지스트리상 죽은 듯 보이나 `construction.py`를 통해 라이브. 삭제 금지.

### 엔진 워크스트림 이관 (defer) — 여기서 미평가
legal_engine_v510, ksic_engine, law_collector, law_catalog_collector,
law_collector_admrul, kosha_collect, law_viewer, documents,
document_schema, document_runtime, document_forms

## 배포·검증
- 방식: Git Data API 단일 원자 커밋(트리 sha:null 삭제) → main ref 이동 → Railway 자동 배포.
- 부팅 확인: 배포 후 `GET /health` → `{"status":"ok","version":"6.0.2"}` (정상). 20개 미참조 고아 삭제로 인한 import 오류 없음.

## 잔여
- 엔진 경계 11개: 엔진 워크스트림에서 폐기버전 정리 합의 후 처리.
- 교육·알림 코드 dedup(O6/O8 겸): 별도 진행.
