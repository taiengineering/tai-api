# OBJ-06b (3) — 데스크톱 32화면 in-place 부트스트랩 전수검증

일자: 2026-07-24 · 대상: `taiengineering/tai-admin`@main `tadmin/full-version/html/horizontal-menu-template/` · READ-ONLY

## 결과: 회귀 0건 (전 화면 리빌드 in-place LIVE)
- 메뉴 링크 32개 중 **31개**가 자기 이름과 일치하는 `‹script type="module" src="../../assets/js/tai-rebuild/pages/<name>/bootstrap.js"›`를 로드. 참조 dir 불일치 0건, 구 인라인 코드 잔존 0건.
- `index.html`은 리다이렉트 스텁(`window.location.replace('safety-dashboard.html')`) — 설계상 정상. 실제 대시보드 `safety-dashboard.html`은 `pages/dashboard/bootstrap.js`를 로드(리빌드 확인). 따라서 진입 경로 전체가 리빌드본.
- 대표 증거: `factory-list.html`→`pages/factory-list/bootstrap.js`, `construction-inspection-anchor.html`→`pages/construction-inspection-anchor/bootstrap.js`, `safety-dashboard.html`→`pages/dashboard/bootstrap.js`.

## 부수 관찰(추가 엔드포인트, 엔진 아님)
safety-dashboard 인라인 위젯(FN-07 미이행/날씨)이 O6-a 표 외 tai-api 엔드포인트 사용: `/overdue/summary`, `/overdue/history`, `/overdue/urge/{id}`, `/weather/now`, `/weather/work-stop-criteria`, `/juso/coord`, `/factories/{id}`, `/construction/sites`. 전부 tai-api·비엔진. (dedup 시 이 인라인 위젯을 모듈로 흡수할지 O6-c에서 검토)

## O6-b 진행
- (2) admin 화면 이관 — 완료(safe에서 engine-monitoring/watch-engine 제거, 커밋 `f2396d5`).
- (3) 부트스트랩 전수검증 — **통과(본 문서)**.
- (4) 모바일 PWA OTP 정식경로·FCM 이관 범위 — 대기.
- (5) admin 공유 코어 dedup(api.js/auth-guard/config) — 대기.
- (6) 런타임 콘솔 보류.
- `.rebuild.html` 32개 = 편집 원본으로 유지(결정).
