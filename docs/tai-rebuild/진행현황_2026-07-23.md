# TAI 리빌드 — 진행현황 (작업내역)

최종 갱신: 2026-07-23 · 배포 방식: main 직접 커밋·배포 · 키: Railway env 기본

## 완료 (실행·배포됨)

### 초기 이슈 수정
- **#1 kin-management 401/42501** — 실 Supabase(`vwlahtguyggrhvslabax`)에서 `naver_kin_log`, `kin_keyword_sets`, `kin_prompt_settings` anon SELECT GRANT.
- **#2 equipment/price-setting "Failed to fetch"**
  - `routers/equipment_assets.py`: `GET /overview`를 `/{asset_id}` 앞에 신규(라우트 순서), `v_equipment_overview` 뷰 조회.
  - price-setting SaaS/진단 탭: `price_saas_plan`·`price_diagnosis_report` 호환 뷰를 `price_master` 위에 생성.

### 가격 SSOT (OBJ-C4) — 정본 A(`price_master`) 단일화
- 현재 서비스 2종(SaaS·법령진단) → **A만 사용**, B/C 격리.
- 격리(archive 스키마, 복구 주석): `price_discount`, `price_region_surcharge`, `price_travel_rule`, `product_pricing`, `price_policy`.
- `routers/pricing_validation_api.py` 재작성: `product_pricing` 교차검증 제거, SSOT=price_master.
- `router_registry/payment.py`: `product_pricing`·`price_policy` 등록 주석 처리.
- 검증: `/public/pricing/saas-plans`가 A 데이터 정상 반환(비파괴).
- 커미션: 개발됐으나 서비스화 장기 → **패스**. 교육·알림: 단일 라이브+휴면+런타임 엔진 라이브 → **DB 격리 불필요**(코드 dedup만 O6/O8).

### 앱 결합·보안 (OBJ-C5)
- `admin-vue3/src/composables/useSupabaseRest.ts`: URL/anon 키를 `import.meta.env`로(폴백 유지).
- `admin-vue3/.env.example` 추가.

### 라우트 순서 린트 (OBJ-03)
- `tests/test_route_order.py`: 동일 (method, parent)에서 동적 `/{param}`이 정적 형제보다 먼저 등록되면 실패.

### 백엔드 데드코드 (OBJ-08) ← 금일
- 미등록 고아 라우터 **import 전수 검증** 후, 앱 소유·참조 0인 **20개 삭제**(단일 원자 커밋 `a12cb3b`).
- construction 서브라우터 3종·`inspection_set_auto`는 라이브 import라 유지. 엔진 경계 11개는 엔진 워크스트림 이관.
- 배포 후 `/health` `{"status":"ok","version":"6.0.2"}` 정상.

### 문서 (tai-api `docs/tai-rebuild/`)
README, 00 아키텍처 심층감사, 01 작업계획서(object, C1~C5), 02 실코드 정밀분석 보정, 03 엔진IO 소비로직 지도, 04 종합결론, 05 추가조사 종결, 06 계획 시뮬레이션, baseline/(OBJ-00·C4·C5·가격 전체플랜·교육알림·OBJ-08).

## 경계·원칙 (고정)
- 계약(C1/O5)은 **제일 마지막**.
- 법령·체크 엔진 = 외부 범용 엔진(블랙박스) → 별도 워크스트림. 엔진 소유 테이블·함수·폐기버전 불가침.
- anon 키 로테이션은 고객의 Supabase/Cloudflare 대시보드 작업. dev_otp env-gate는 별도 auth PR(로그인 장애 위험으로 직접 배포 금지).

## 남은 로드맵
- **O6** — safe(tadmin) 리빌드 + admin 잔여 + 레거시 은퇴, 교육·알림 코드 dedup. ← 다음
- **O7** — DB 격리 마감(엔진 경계 제외), C1/C2 게이트.
- **O9/O10** — 후속.
- **C1/O5(계약)** — 최종.
