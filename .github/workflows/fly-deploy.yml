# DEPRECATED: Fly.io → Railway 이전 완료 (2026-04-20)
# Railway는 GitHub 연결로 main push 시 자동 배포
# 이 파일은 더 이상 사용되지 않음

name: "[DISABLED] Deploy to Fly.io"
on:
  workflow_dispatch:
    inputs:
      note:
        description: 'This workflow is disabled. Railway auto-deploys on main push.'
        required: false

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Fly.io deployment disabled. Railway auto-deploys from main branch.'
